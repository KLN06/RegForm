@{
    ViewData["Title"] = "Home Page";
}
<div class="text-center">
    @if (User.Identity.IsAuthenticated)
    {
        <h1 class="display-4">Welcome, @User.Identity.Name!</h1>
    }else
    {
        <h1 class="display-4">You can log in or register.</h1>
    }
</div>
